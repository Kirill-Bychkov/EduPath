import { COLORS } from "../constants/colors"

export const lightColors = {
    primary: COLORS.primary,
    background: COLORS.light_background,
    bar_background: COLORS.bar_light_background,
    header_text: COLORS.Header_light_text,
    text: COLORS.light_text,
    not_active: COLORS.not_active,
}

export const darkColors = {
    primary: COLORS.primary,
    background: COLORS.dark_background,
    bar_background: COLORS.bar_dark_background,
    header_text: COLORS.Header_dark_text,
    text: COLORS.dark_text,
    not_active: COLORS.not_active,
}