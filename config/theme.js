import { COLORS } from "../constants"

export const lightColors = {
    primary: COLORS.EDUCATION.primary,
    background: COLORS.EDUCATION.light_background,
    bar_background: COLORS.EDUCATION.bar_light_background,
    header_text: COLORS.EDUCATION.Header_light_text,
    text: COLORS.EDUCATION.light_text,
    not_active: COLORS.EDUCATION.not_active,
    code_editor: COLORS.INTERPRETER.code_editor.light,
    text_interpreter: COLORS.INTERPRETER.text.light.default,
    text_error_interpreter: COLORS.INTERPRETER.text.light.error,
    text_success_interpreter: COLORS.INTERPRETER.text.light.success,
    background_interpreter: COLORS.INTERPRETER.background.light,
    placeholder_interpreter: COLORS.INTERPRETER.placeholder.light
};

export const darkColors = {
    primary: COLORS.EDUCATION.primary,
    background: COLORS.EDUCATION.dark_background,
    bar_background: COLORS.EDUCATION.bar_dark_background,
    header_text: COLORS.EDUCATION.Header_dark_text,
    text: COLORS.EDUCATION.dark_text,
    not_active: COLORS.EDUCATION.not_active,
    code_editor: COLORS.INTERPRETER.code_editor.dark,
    text_interpreter: COLORS.INTERPRETER.text.dark.default,
    text_error_interpreter: COLORS.INTERPRETER.text.dark.error,
    text_success_interpreter: COLORS.INTERPRETER.text.dark.success,
    background_interpreter: COLORS.INTERPRETER.background.dark,
    placeholder_interpreter: COLORS.INTERPRETER.placeholder.dark
};
